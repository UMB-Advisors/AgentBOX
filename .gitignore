# Redundant source archives (extracted alongside as folders)
*.zip

# Dependencies
node_modules/
.venv/
venv/
__pycache__/
*.pyc

# Build artifacts
build/
dist/
.next/
target/

# Nix
result
result-*

# Env files (examples are kept)
.env
.env.local
.env.*.local
!.env.example
!.env.*.example

# OS / editor
.DS_Store
*.swp
.direnv/

# Claude Code agent worktrees (local session state, not source).
# ``**/`` so it matches nested checkouts, not just the repo root.
**/.claude/worktrees/

# Agent controller scratch (browse logs, run state) — never source.
.gstack/
.supergoal/
.goal/

# entity_map.yaml retired here — canonical source is UMB-Advisors/agentbox-seed
# (entities/entity_map.yaml). Local working copy is a symlink to it; the box's
# ~/gbrain-ingest copy is deployed from the seed (apply --module entities).
gbrain-ingest/entity_map.yaml
