- agentboxhonduras-access (RETARGET 2026-07-07: this run now targets agentboxhonduras, the developer's physically-accessible unit — keyboard+monitor on hand for lockout recovery; the original agentbox2 was remote and unrecoverable for this developer). Reachable `ssh agentboxhonduras` (user `carlos`; also `carlos@100.117.25.5`); sidecar on :9200. **Serve/Funnel state is NOT assumed** — unlike agentbox2, agentboxhonduras may have no Tailscale Serve (:9120) or Funnel (:443) configured; Phase 1 CAPTURES the actual pre-change Serve/Funnel state and asserts it's unchanged after re-tag (capture-then-compare, no hardcoded :9120==200). Note: agentboxhonduras was the OOBE bench box — **verify the sidecar is actually serving the dashboard on :9200 before Phase 3** (it may be mid-onboarding or on a different branch build). Changes: phase specs use these endpoints + capture-then-compare for Serve/Funnel.
- agentbox1-access — agentbox1 repurposed (jenny-ai); NOT a target for this spike. Changes: all box work targets agentboxhonduras only.
